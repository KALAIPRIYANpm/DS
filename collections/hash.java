package collections;

import java.util.HashSet;

public class hash {
    public static void main(String[] args) {
        HashSet<Integer>arr=new HashSet<>();
        arr.add(2);
        arr.add(3);
        System.out.println(arr);
        arr.remove(2);
        System.out.println(arr);
    }
}

//the above mentioned is same as other sets
